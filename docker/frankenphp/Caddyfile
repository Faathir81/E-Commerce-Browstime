{
    frankenphp
    # Disable admin endpoint (not needed in production)
    admin off
}

:80 {
    # Laravel's document root is the /public folder
    root * /app/public

    # Enable compression
    encode zstd br gzip

    # Serve PHP files via FrankenPHP
    php_server
}
